# Instructions Copilot pour ha_3Dqueue

## Commande de Release

Quand l'utilisateur dit **"release X.Y.Z"** (exemple: "release 1.0.1"), exécuter TOUTES ces étapes automatiquement SANS demander confirmation :

### 1. Mettre à jour les versions dans les fichiers

- `config.yaml` : champ `version`
- `README.md` : ligne `**Version X.Y.Z**`
- `build.yaml` : section `args.version` si présent

### 2. Créer/mettre à jour CHANGELOG.md

Créer le fichier s'il n'existe pas, ou ajouter en haut :

```markdown
## [X.Y.Z] - YYYY-MM-DD

### Added
- [Liste des nouvelles fonctionnalités]

### Changed
- [Liste des modifications]

### Fixed
- [Liste des corrections de bugs]
```

Utiliser la date du jour au format ISO (ex: 2025-11-22).

Si aucun changement n'est spécifié par l'utilisateur, demander brièvement "Quels changements pour cette version ?" AVANT de commencer le processus.

### 3. Git commit

```bash
git add .
git commit -m "Release vX.Y.Z"
```

### 4. Créer le tag Git

```bash
git tag -a vX.Y.Z -m "Version X.Y.Z"
```

### 5. Push vers GitHub

```bash
git push origin main
git push origin vX.Y.Z
```

### 6. Afficher un résumé

Afficher :
- ✅ Version mise à jour dans tous les fichiers
- ✅ CHANGELOG.md créé/mis à jour
- ✅ Commit créé : "Release vX.Y.Z"
- ✅ Tag créé : vX.Y.Z
- ✅ Poussé vers origin/main et tag vX.Y.Z
- 🎉 Release X.Y.Z terminée !

## Notes importantes

- **NE PAS** demander de confirmation pour chaque étape
- **NE PAS** demander "Voulez-vous que je..." 
- **FAIRE** toutes les étapes automatiquement en séquence
- Si une erreur survient, s'arrêter et rapporter l'erreur
- Vérifier que tous les fichiers sont sauvegardés avant le commit

## Exemple d'exécution

```
Utilisateur: "release 1.0.1"

Copilot:
- Met à jour config.yaml version: "1.0.1"
- Met à jour README.md **Version 1.0.1**
- Crée CHANGELOG.md avec entrée [1.0.1] - 2025-11-22
- git add .
- git commit -m "Release v1.0.1"
- git tag -a v1.0.1 -m "Version 1.0.1"
- git push origin main
- git push origin v1.0.1
- Affiche le résumé de succès
```

## Fichiers à vérifier pour les versions

- `/home/guillaume@flace.lan/Documents/dev/ha/ha_3Dqueue/config.yaml`
- `/home/guillaume@flace.lan/Documents/dev/ha/ha_3Dqueue/README.md`
- `/home/guillaume@flace.lan/Documents/dev/ha/ha_3Dqueue/build.yaml`
- `/home/guillaume@flace.lan/Documents/dev/ha/ha_3Dqueue/CHANGELOG.md` (créer si absent)
